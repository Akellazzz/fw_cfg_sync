names: 
  action: check
  template: ^names$
no_names: 
  action: check  
  template: ^no\snames$
name: 
  action: check  
  template: ^name\s
object_not_for_nat:
  action: check  
  template: 
    parentspec: ^object\s
    childspec: ^\snat\s
    find_obj_mode: without
object_for_nat:
  action: check  
  template:   
    parentspec: ^object\s
    childspec: ^\snat\s
    find_obj_mode: with
object-group: 
  action: check  
  template: ^object-group\s
time-range: 
  action: check  
  template: ^time-range\s
access-list: 
  action: check  
  template: ^access-list\s
class-map: 
  action: check  
  template: ^class-map\s
flow-export: 
  action: check  
  template: ^flow-export\s
policy-map: 
  action: check  
  template: ^policy-map\s
service-policy: 
  action: check  
  template: ^service-policy\s
access-group: 
  action: check  
  template: ^access-group\s
nat: 
  action: check  
  template: ^nat\s
